rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ログインした本人だけが自分のデータ(users/{自分のuid}配下)を読み書きできる
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
